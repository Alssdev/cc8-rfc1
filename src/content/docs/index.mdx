---
title: "Primeros Pasos"
description: "En esta página se explica la instalación del SDK de la Raspberry PI Pico y otras herramientas."
---

import { Aside } from '@astrojs/starlight/components';

## 📋 Requisitos

Antes de comenzar, sigue los pasos de [esta guía](https://piers.rocks/2023/09/05/installing-pico-sdk-and-picotool.html) para obtener
todas las dependencias. Luego de completar los pasos, deberías tener instalado:

- cmake
- make
- pico SDK
- picotool

<Aside>
  También puedes utilizar una [extensión de VSCode](https://datasheets.raspberrypi.com/pico/getting-started-with-pico.pdf) para desarrollar este proyecto.
</Aside>
<Aside type="caution">
  Asegurate de configurar `PICO_SDK_PATH` en tus variables de entorno.
</Aside>

## 🛠️ Obtener el código

Sigue los siguientes pasos para obtener una copia del código base de este proyecto.
```bash
git clone https://github.com/Alssdev/raspberry-pi-pico-spi-daisy-chain.git
cd raspberry-pi-pico-spi-daisy-chain
```

Luego, abre el archivo `Makefile` y modifica la siguiente linea.
```makefile
# set pico board
PICO_BOARD=pico_w       # pico_w or pico
```

También debes modificar el archivo `CMakeList.txt` para configurar el tipo de board que usaras.
```cmake
target_link_libraries(main
  pico_stdlib
  # Uncomment when using Pico W
  pico_cyw43_arch_none        # comment if you are NOT using a pico_w
  pico_multicore
  hardware_spi
  hardware_uart
)
```

Finalmente, ejecuta el siguiente comando para compilar y asegurarte que todo está bien:
```bash
make
```

Ya podemos pasar a la parte de cargar código a la PI Pico.

## 🚀 Flashing your board

Para cargar el código compilado en la PI Pico esta base de código incluye un comando útil.
Conecta tu PI Pico en **flash mode**, luego ejecuta el siguiente comando:

```bash
make flash
````
Ahora estás listo para escribir código! 🎉
